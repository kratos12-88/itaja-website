import Link from 'next/link';
import { pricing } from '@/lib/site';
export default function Pricing(){return <><section className="page-hero"><div className="container"><span className="eyebrow">Pricing</span><h1>Simple monthly plans for small sellers.</h1><p>Start with the basics, upgrade when you need inventory depth or payment features, and avoid long billing lock-ins.</p></div></section><section className="section section-light"><div className="container"><div className="pricing-grid">{pricing.map(plan=><article className={`price-card ${plan.featured?'featured':''}`} key={plan.name}><h3>{plan.name}</h3><div className="price">{plan.price}</div><div className="price-sub">{plan.suffix}</div><p>{plan.description}</p><ul className="price-list">{plan.features.map(f=><li key={f}><span className="tick">✓</span>{f}</li>)}</ul><Link href="/#join" className={`btn ${plan.featured?'':'btn-primary'}`}>Join early access</Link></article>)}</div><div className="service-panel" style={{marginTop:48}}><h3 style={{marginTop:0,color:'var(--oxblood)'}}>Done-for-you management</h3><p style={{color:'var(--muted)',lineHeight:1.7}}>For vendors who prefer not to self-serve, Itaja can update products for you. Pricing is based on workload: roughly ₦200–₦500 per product update, or monthly volume tiers starting around ₦2,000.</p></div></div></section></>}
